/**
 * Caclulates play efficiency given a list of entries
 * @param {object[]} playData 
 * @param {string} posession
 * 
 * @returns {object[]} - List of plays that includes name, efficiency, lastFiveOutcomes[]
 */
const calculatePlayEfficiency = (playData, possession) => {
  // Points Scored per Play / Total Uses
  console.log(`Possession: ${possession} - invert if defense`);
  const efficiencies = {}
  console.log(playData)
};